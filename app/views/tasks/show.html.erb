<h1><%= @task.title %></h1>
<h2><%= @task.details %></h2>

<% status = @task.completed ? 'fa-square-check' : 'fa-square' %>

<i class="fa-regular <%= status %>"></i>
<p><%= @task.completed ? 'This task is completed' : 'This task is not completed yet' %></p>

<br>
<hr>
<%= link_to "home", tasks_path %>
